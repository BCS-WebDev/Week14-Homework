
import React from 'react';

function Thumbnail(props) {
    return (
        <a>
            <img className={props.className} src={props.src} onClick={} style={{ height: 50 }}/>
        </a>
    );
}
  
export default Thumbnail;
  